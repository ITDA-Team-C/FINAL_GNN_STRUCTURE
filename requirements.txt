# Core (preliminary submission scope)
pandas==2.3.3
numpy==2.4.3
scikit-learn==1.8.0
pyyaml==6.0.2
scipy==1.17.1

# Deep learning (GPU recommended)
torch==2.11.0
torch-geometric==2.7.0

# torch-geometric optional accelerators (install matching your CUDA/torch).
# Example for torch 2.11 + CUDA 12.1:
#   pip install pyg-lib torch-scatter torch-sparse \
#     -f https://data.pyg.org/whl/torch-2.11.0+cu121.html
# For CPU-only:
#   pip install pyg-lib torch-scatter torch-sparse \
#     -f https://data.pyg.org/whl/torch-2.11.0+cpu.html
